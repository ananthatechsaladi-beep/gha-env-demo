name: Input Demo

on:
  workflow_dispatch:
    inputs:
      enviorment:
        description: Choose the environment to deploy
        required: true
        default: dev
jobs:
  inputs-demo:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy the APP
        run: echo "Deploying to the application to ${{ inputs.enviorment }}"
